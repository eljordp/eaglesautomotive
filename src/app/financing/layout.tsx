import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Financing — Get Approved Today",
  description:
    "Apply for auto financing at Eagles Automotive Inc. We work with all credit types. Fast approvals and competitive rates in Murfreesboro, TN.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
